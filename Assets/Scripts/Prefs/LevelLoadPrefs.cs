namespace Prefs
{
    public static class LevelLoadPrefs
    {
        private const string _completedLevels = "CompletedLevels";
        private const string _startsCount = "StartsCount";

        public static string CompletedLevels = _completedLevels;
        public static string StartsCount = _startsCount;
    }
}